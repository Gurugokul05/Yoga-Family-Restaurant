import { getFirestore,updateDoc,doc } from "firebase/firestore";

const db = getFirestore();


export const changeStatus = async(itemId,itemValue) => {
    const foodDoc = doc(db,)
  return (
    <div>changeStatus</div>
  )
}
